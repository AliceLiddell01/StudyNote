**Пояснение**: `git branch` создаёт, lists или удаляет ветки.

**Пример**:
- Создать: `git branch feature-branch`
- List: `git branch` (покажет * master, feature-branch).
- Удалить: `git branch -d feature-branch`