**Пояснение**: `git switch` — современный способ переключения на ветку (с Git 2.23+), safer чем [[checkout]].

**Пример**: `git switch feature-branch`. Если ветки нет, `git switch -c new-branch` создаст и переключит.