**`HAVING`**: Фильтрует результат `GROUP BY`. [[WHERE]] фильтрует строки _до_ группировки, а `HAVING` — сгруппированные данные _после_.

![[Pasted image 20250810203021.png]]
(Выводит города с более чем 5 пользователями.)